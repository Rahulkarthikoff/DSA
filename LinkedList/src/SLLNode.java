public class SLLNode{
    int data;
    SLLNode next;

    // it can be with this constructor or without constructor also
    public SLLNode(int data)
    {
        this.data = data;
        this.next = null;
    }
}